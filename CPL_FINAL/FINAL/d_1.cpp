#include<iostream>
#include<string>
using namespace std;
int main()
{
    long long n;
    string a;
    int i,flag,j;
    while(1)
    {
        cin >>n;
        if (n==0)
            break;
        else
        {
            cin>>a;
            for (i=0;i<n;i++)
            {
                if (size_t found = str.find(Z))
                {
                    cout<<"0\n";
                    flag = -1;
                    break;
                }
                else if(a[i]=='R'||a[i]=='D')
                {
                    for (j=0;j<n;j++)
                    {

                    }
                }
        }
    }
    return 0;
}

if(a[i]=='R'&&a[i+1]=='D')
                        {
                            cout<<"1\n";
                            flag = -1;
                            break;
                        }
                        if(a[i]=='D'&&a[i+1]=='R')
                        {
                            cout<<"1\n";
                            flag = -1;
                            break;
                        }
